export default function NotFound () {
    return <main className="not-found">
        <h1>meals not found!</h1>
        <p>Unfortunately, we could not find the requested page or meals data!</p>
    </main>
}